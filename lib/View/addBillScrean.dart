import 'package:flutter/material.dart';

class AddNewBillScrean extends StatelessWidget{

  @override
  Widget build(BuildContext context){
    return Scaffold(
      body: Column(
        children: <Widget>[
          TextField(),
          TextField(),
          TextField(),
          TextField(),
        ],
      ),
    );
  }
}